
// Importing from Instance type for backward compatibility
import { Instance } from "./instance";

// Exporting Installation as an alias for Instance to maintain compatibility
export type Installation = Instance;
